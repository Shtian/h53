# Contracts – Global Cabin Header

No new HTTP endpoints or Convex functions are introduced for this feature. Navigation remains client-side within the authenticated Next.js App Router, and weather data continues to be sourced via the existing `fetchYrNoSnapshot` helper with server-side caching.
