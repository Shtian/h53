import { test } from "@playwright/test";

test("family member can log in with Clerk and browse album", async () => {
  throw new Error(
    "Implement Clerk login + album browsing flow before enabling this check"
  );
});
