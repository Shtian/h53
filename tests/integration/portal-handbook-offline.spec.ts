import { test } from "@playwright/test";

test("handbook articles available offline after caching", async () => {
  throw new Error(
    "Implement handbook offline caching flow before enabling this check"
  );
});
